<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_upwork_skill_tag_list" model="ir.ui.view">
        <field name="name">upwork.skill.tag.list</field>
        <field name="model">upwork.skill.tag</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="category_id"/>
                <field name="crm_tag_id"/>
                <field name="color" widget="color_picker"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_upwork_skill_tag_form" model="ir.ui.view">
        <field name="name">upwork.skill.tag.form</field>
        <field name="model">upwork.skill.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="category_id"/>
                        <field name="crm_tag_id"/>
                        <field name="color" widget="color_picker"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_upwork_skill_tag" model="ir.actions.act_window">
        <field name="name">Skill Tags</field>
        <field name="res_model">upwork.skill.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
